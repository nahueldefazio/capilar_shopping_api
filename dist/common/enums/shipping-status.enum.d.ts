export declare enum ShippingStatus {
    PENDING = "pending",
    PREPARING = "preparing",
    LABEL_CREATED = "label_created",
    SHIPPED = "shipped",
    DELIVERED = "delivered",
    CANCELLED = "cancelled"
}
